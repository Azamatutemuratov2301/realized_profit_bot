services:
  - type: web
    name: realized_profit_bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    plan: free
    branch: main
    autoDeploy: true